import { RegistrationFormModel } from './registration-form.model';

describe('RegistrationForm', () => {
  it('should create an instance', () => {
    expect(new RegistrationFormModel()).toBeTruthy();
  });
});
